/**
 * PathEvaluator.js
 * 
 * Реализация класса tuna.tmpl.PathEvaluator.
 * 
 * @author Kononenko Sergey <kononenheg@gmail.com>
 */

if (!COMPILED) {
	tuna.namespace("tuna.tmpl");
}

/**
 * Класс объектов обработки выборок (путей) в наборе данных.
 *
 * @public
 * @class
 * @constructor
 */
tuna.tmpl.PathEvaluator = function() {
	
};

/**
 * Компиляция строки выборки во внутренеее правило выбора.
 *
 * @public
 * @param {string} path Строка выборки.
 */
tuna.tmpl.PathEvaluator.prototype.compile = function(path) {

};


/**
 * Применение правила выбора к данным.
 *
 * @public
 * @param {Object} data Объект данных.
 * @return {Object} Результат выборки.
 */
tuna.tmpl.PathEvaluator.prototype.evaluate = function(data) {
	return null;
};
